import { useDispatch } from "react-redux"
import { fetchFail, fetchStart, getUser } from "../features/blogSlice"


const useBlogCalls = () => {
    const dispatch = useDispatch()
const getBlogs = async()=>{
    
}
}

export default useBlogCalls